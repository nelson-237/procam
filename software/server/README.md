# Procam server

## Installation:
- install nodejs
- install npm 
- run `npm install`

## Launch the web server:
- run `nodejs src/server.js`

## Configuration:
- change the configuration in `src/config.json`
